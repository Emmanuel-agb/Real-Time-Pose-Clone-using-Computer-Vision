# Real-Time-Pose-Clone-using-Computer-Vision
I built a Python-based pose estimation engine using OpenCV and MediaPipe that implements exponential moving average (EMA) smoothing and Gaussian bloom rendering to create a high-fidelity digital twin.
